## 1. The item as a script

- [x] 1.1 Write `menubar/sidescreen-menubar.js`, a JavaScript for Automation script: the two JXA rules named at the top (no `Ref()` for object out-parameters, no blocks); argument parsing for `--port`, `--state-dir`, `--node`, `--bin`, and `--version`, the last reading the version from `package.json` two directories up; the single-instance check through `pgrep -f` for another `osascript` on the same script and state directory, excluding its own pid, exiting 0 quietly; the registered controller class; the status item with the `macwindow.on.rectangle` and `rectangle.on.rectangle.slash` template symbols; the menu rebuilt per state with "SideScreen is running" or "SideScreen is stopped", Open SideScreen, Start, Stop, Show log, and Quit enabled per the spec, no keyboard shortcuts; the two-second poll through a synchronous request with a one-second timeout and null out-parameters; Start and Stop through `NSTask` with the inherited environment, completion through `NSTaskDidTerminateNotification`, the first non-empty stderr line on failure; the tooltip with port, version, and pid; the accessory activation policy; verify by running it by hand against a server on a test port and seeing the running entry, then `--version` printing the package version
- [x] 1.2 Keep the Swift helper as the future signed option: add a comment at the top of `native/menubar/main.swift` saying why it is not shipped and how to switch it in (build, sign, notarize, `SIDESCREEN_MENUBAR_BIN`), leave `scripts/build-menubar.sh` and the `build:menubar` script in place, remove `native` from `files`, and set `prepublishOnly` back to `npm run check`; verify with `npm pack --dry-run` that the tarball lists `menubar/sidescreen-menubar.js`, nothing under `native/`, and nothing under `test/` or `openspec/`

## 2. Launching from start

- [x] 2.1 Rework `src/lifecycle/menubar.js`: keep `menubarDecision`; add `menubarCommand(env)` returning `{ file, args }` for the default (`/usr/bin/osascript`, `['-l', 'JavaScript', <script>]`) and for `SIDESCREEN_MENUBAR_BIN` (the program, no prefix), with the four item arguments appended by the launcher; the launcher checks the shipped script exists and writes one stderr line naming it when it does not, appends to `<state dir>/menubar.log`, and spawns detached with `unref()`; verify with unit tests for the decision on darwin, darwin with `SIDESCREEN_MENUBAR=off`, and linux, for both command shapes, and for the launch through a fake item that records its arguments and environment, plus the missing-script note
- [x] 2.2 Keep the call from `startCommand` after every successful report; verify with CLI tests on darwin that `start` with `SIDESCREEN_MENUBAR_BIN` set to the fake records exactly one launch carrying the port, the state directory, the running node path, and the bin path, with `SIDESCREEN_STATE_DIR` in the recorded environment; that a second `start` records a second launch, since deduplication is the item's; that `SIDESCREEN_MENUBAR=off` records nothing and prints nothing about the item; and that a nonexistent `SIDESCREEN_MENUBAR_BIN` yields one stderr line and exit 0
- [x] 2.3 Update the usage text and the README: `SIDESCREEN_MENUBAR` and `SIDESCREEN_MENUBAR_BIN` in both, and the section "The menu bar icon (macOS)" saying how the item is run, what the menu offers, that Quit leaves the server running, that `sidescreen start` brings the icon back, that the icon acts with the environment it was launched with, where `menubar.log` lives, and that Linux and Windows get no icon and no mention of one; verify by reading it against every requirement in the `macos-menu-bar` delta

## 3. End to end and checks

- [x] 3.1 Add a smoke test that, on macOS, runs `osascript -l JavaScript menubar/sidescreen-menubar.js --version` and asserts the package version, skipped with a reason elsewhere; keep the Swift helper's smoke test, skipped while it is unbuilt; verify that both report as expected on this machine
- [x] 3.2 Walk the spec by hand on this machine: `sidescreen start`; the icon appears, the first entry reads "SideScreen is running", and the tooltip names the port, version, and pid; Stop from the menu turns the entry to "SideScreen is stopped" within a few seconds and offers Start; Start from the menu brings the running entry back with a new pid in the tooltip; Open SideScreen opens the browser on the server; `sidescreen stop` in a terminal turns the entry to "SideScreen is stopped"; Start while another program holds the port shows that command's first error line and offers Start again; `sidescreen start` again with the icon running leaves one icon; Quit leaves the server answering and `sidescreen start` brings the icon back; record the date and outcome in this task. Partial outcome, 2026-09-17 on a Jamf-managed Mac running CrowdStrike Falcon: the compiled Swift helper was quarantined on execution, which is why the item is now the script; `sidescreen start` on the default port and store launched the script item, which stayed after the command exited, showed the running entry, polled health every two seconds, and was left alone by Falcon; a second `start` left one item, the newcomer logging "another item already runs" with the survivor's pid; `--version` prints the package version. Not yet exercised by hand: the menu itself (Open SideScreen, Stop, Start, Show log, the failure line, and Quit), which needs a person at the screen; completed by hand on 2026-09-17 with the script item: every menu entry behaved as the spec describes
- [x] 3.3 Run `npm run check` and confirm lint, typecheck, and all tests pass. Outcome, 2026-09-17: passed; the Swift helper's smoke test reports itself skipped while unbuilt
