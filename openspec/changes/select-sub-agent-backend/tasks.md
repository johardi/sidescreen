## 1. Store, types, and ingest

- [x] 1.1 Add `model` to the `Turn` type and `backend` to the `Exchange` type, remove the thread-level `subAgentSessionId` copy, move the store to version 3, and normalize a version 2 store on first read by setting `model: null` on every turn and `backend: 'codex'` on every exchange that holds a session id, writing the one-time backup as the version 1 migration does; verify with store tests that a version 2 fixture opens, every old exchange reads `codex`, threads and carry-back are intact, and the backup is written once
- [x] 1.2 Add a transcript model reader beside the title reader that returns the `message.model` of the last `assistant` record in the transcript tail, or null for any failure, and record it on the turn at ingest; verify with unit tests for a model present, no assistant record, a missing file, and malformed lines, and with an ingest test that the stored turn carries the model
- [x] 1.3 Make `sidescreen ingest` and `sidescreen carry-back --emit` exit successfully and do nothing when `SIDESCREEN_SUBAGENT=1` is set; verify with CLI tests that a payload under the marker stores no turn and emits no carry-back, and that the pending entries remain for the next unmarked prompt

## 2. Backend adapters

- [x] 2.1 Define the adapter interface (command for a new session, command for a fork, output parsing to session id, final text, and errors, and fixed read-only arguments) and move the existing Codex command construction and event parsing behind it unchanged; verify that every existing dispatch test still passes against the Codex adapter
- [x] 2.2 Write the Claude adapter: `claude -p --restricted --tools Read,Grep,Glob --permission-prompts none --output-format json --json-schema <schema text> --add-dir <transcript directory>`, `--model` when a model is known, `--name "sidescreen: <question>"`, `--resume <id> --fork-session` for a fork, the prompt as the last argument, `SIDESCREEN_SUBAGENT=1` in the child environment, and parsing of `structured_output`, `result`, `is_error`, `session_id`, and `permission_denials`; verify with unit tests that the read-only flags are present in both command shapes, that no parameter can remove them, and that each result shape parses as designed
- [x] 2.3 Add `test/fixtures/stub-claude.js` speaking the print-mode result format, minting `fork-of-<id>-<pid>` for a fork, recording arguments and prompt to files, and exiting non-zero when stdin stays open; verify with a dispatch test that a new question and a fork through the stub yield the expected session lineage and a closed stdin
- [x] 2.4 Replace the dispatch settings with `SIDESCREEN_SUBAGENT` (`parent`, `claude`, `codex`; default `parent` resolving to `claude`), `SIDESCREEN_CLAUDE_BIN`, `SIDESCREEN_CODEX_BIN`, `SIDESCREEN_CLAUDE_MODEL`, and `SIDESCREEN_CODEX_MODEL`, with an explicit Claude model winning over the turn's recorded model, and remove `SIDESCREEN_MODEL`; verify with unit tests for each variable, the default resolution, and the model precedence
- [x] 2.5 Assert prompt parity: dispatch the same first question once per backend through the two stubs and compare the recorded prompts byte for byte, and assert that neither recorded command line carries a system prompt, agent definition, or instruction file argument; verify that this test passes and fails when a backend-only flag is added to either adapter
- [x] 2.6 Make the dispatcher take a backend and a fork target, pick the adapter, pass the turn's model to the Claude adapter, and report a CLI that cannot be started with a message naming the backend and its `_BIN` setting; verify with dispatch tests that a missing Codex binary fails with that message, that no other adapter is tried, and that both stubs answer end to end

## 3. Tag parsing and the fork rule

- [x] 3.1 Add the tag parser: the first `@claude` or `@codex` standing as a word of its own, in any case and at any position, yields the backend and the question with that token removed; an address, a domain, an escaped tag, or an unknown name yields no backend and the text unchanged; verify with unit tests for both tags, mixed case, a leading tag, a tag mid-sentence, a tag before punctuation, two tags, an address, a domain, an escaped tag, an unknown tag, and a tag with no question after it
- [x] 3.2 Add the lineage function over the thread tree: a thread's exchanges in order for a follow-up, and for a branch the parent's lineage cut at the branched-from exchange, recursively; verify with unit tests on a three-level tree that each lineage holds exactly the expected exchanges in order
- [x] 3.3 Add the routing function: backend from the tag, else from the last answered exchange in the lineage, else the configured default; fork target as the newest same-backend answered exchange in the lineage with the exchanges after it as the gap, or a cold start with the whole lineage; verify with unit tests for a first question, a same-backend follow-up (empty gap), a switch with no same-backend ancestor (cold start with all exchanges), a switch back (fork of the earlier session with the gap), and a tagged branch that must not see exchanges after its branch point
- [x] 3.4 Extend the prompt builders: the cold-start prompt renders `priorExchanges` under "Earlier in this thread" as question, answer text, source, and detail, and the follow-up prompt gains a "Since your last answer" section naming the backend that produced the gap; verify with snapshot-style unit tests that both sections appear only when there is something to say and that the stripped question is used
- [x] 3.5 Wire the three question routes (new thread, follow-up, branch) through the parser and the routing function, store the verbatim question and the backend on the exchange before dispatch, and pass the fork target and gap to the dispatcher; verify with server tests against both stubs for the leading-tag, mid-sentence-tag, sticky follow-up, switch, switch-back, and tagged-branch scenarios of the spec, asserting the stub logs show which session was forked and what the prompt carried

## 4. The turn slice

- [x] 4.1 Add a transcript module that finds the latest `assistant` record whose text equals the turn's message after whitespace normalization, walks `parentUuid` to the nearest real user prompt, and returns the records between, or null; verify with unit tests on a fixture transcript for a found turn, a turn whose final message appears twice, a transcript that lacks the turn, and a missing file
- [x] 4.2 Render the slice to `<state dir>/turn-slices/<prompt id>.md` with the user prompt, reasoning blocks where present, each tool call with its input and the first 4,000 characters of its result with the omitted length noted, and the final message; verify with unit tests that a 5,000-character tool result is cut at 4,000 and annotated with the omitted length and that the file is rewritten on each call
- [x] 4.3 Make every cold start request the slice and give the cold-start prompt the slice path under the `transcript` source when found, or the not-located notice with the search recipe when not; verify with dispatch tests that the stub's recorded prompt names the slice file in one case and carries the notice in the other, and that the question is dispatched in both

## 5. Browser surface

- [x] 5.1 Show the backend on each answer: a muted line above the answer body reading "Claude answered:" or "CODEX answered:" with the model in its `title`, "Asking Claude…" or "Asking CODEX…" while pending, and the follow-up placeholder "Ask a follow-up… (@claude or @codex to switch)"; verify with browser tests that a thread with one answer from each backend shows the two labels above their answers, that the Claude label's tooltip names the model, and that the pending line names the backend before the answer arrives
- [x] 5.2 Present the backend and model in the thread API responses so the client can render them; verify with a server test that an exchange's presentation carries `backend` and the turn's `model`
- [x] 5.3 Refuse `DELETE /api/turns/<id>` for a session's newest turn with a conflict status and a message saying only past turns can be removed; verify with server tests that the newest turn is refused and remains, that an older turn is still removed, and that a turn becomes removable once a newer turn is ingested
- [x] 5.4 Omit the remove control on the row of each session's newest turn and keep it on past turns, relying on the existing live sidebar re-render when a newer turn arrives; verify with a browser test that hovering the newest row shows no control, hovering an older row does, and ingesting a newer turn makes the previous newest row removable without a reload

## 6. A turn's directory is its session's

- [x] 6.1 Make ingest record the session's directory on a turn whose session already exists, and make the version 3 normalization rewrite every existing turn's directory to its session's; verify with ingest and store tests that a turn arriving from a subdirectory is stored under the session's directory, and that a version 2 fixture with a mismatched turn opens with the turn repaired
- [x] 6.2 Derive the canonical turn address, the `/turns/<id>` redirect, the removal response's next link, and the project page's newest-turn lookup from the turn's session rather than from the turn alone; verify with server tests on a fixture whose turn directory is a subdirectory of its session's that the sidebar link answers 200 with no redirect, the legacy link redirects to the session's project, the project page shows that turn when it is the newest, and no address answers "Project not found"
- [x] 6.3 Confirm the sub-agent for such a turn starts in the project root; verify with a dispatch test through the Claude stub, which records its working directory, that the recorded directory is the session's and not the subdirectory
- [x] 6.4 Reproduce the bug end to end before fixing and pass afterwards: ingest two turns of one session through the real CLI, the second with a subdirectory as `cwd`, open the project in the browser, click the older turn in the sidebar, and assert the document renders rather than the not-found page; verify that the test fails on the current code and passes with 6.1 and 6.2

## 7. Documentation, end to end, and checks

- [x] 7.1 Update the CLI help's environment section and the `serve` start-up line to name the default backend and both CLIs; verify with CLI tests that the help lists the new variables and not `SIDESCREEN_MODEL`
- [x] 7.2 Rewrite the README: describe coding agent responses and a read-only sub-agent, keep Claude Code as the supported harness, move the Codex CLI to an optional requirement for `@codex`, document the tags and the sticky rule, replace the configuration table, state each backend's read-only guarantee in the privacy section, mention the named sub-agent sessions, and say that a session's newest response cannot be removed; verify by reading it against every requirement in the spec delta
- [x] 7.3 Update `package.json`: keep the description agnostic about who answers and add `codex` to the keywords; verify with `npm pack --dry-run` that the manifest is valid
- [x] 7.4 Extend the end-to-end loop test to run once against each stub and once with a mid-thread switch, asserting the sourced answer, the backend chip, and the stub logs; verify that the test passes headless
- [x] 7.5 Add the opt-in real-CLI test under `SIDESCREEN_E2E_CLAUDE=1` that asks a restricted Claude sub-agent to read a file in an added directory, create a file, and run a shell command, and asserts the file is read, nothing is written, and no shell tool was available; verify by running it once on this machine and recording the outcome in the task. Outcome, 2026-09-17 on Claude Code 2.1.274 with haiku: passed twice. The first run read the added directory, wrote nothing, had no shell, and fired no hook, but its tool list still held the user's MCP servers, since `--tools` governs the built-in set only; `--strict-mcp-config` was added to the fixed read-only arguments, and the second run reported exactly Glob, Grep, Read, and StructuredOutput
- [x] 7.6 Run `npm run check` and confirm lint, typecheck, and all tests pass
