## 1. The server identifies itself

- [x] 1.1 Add `GET /api/health` to the server answering `{ name: "sidescreen", version, pid, stateDir }` with no caching, routed before the `/api/` not-found fallback; verify with server tests that the four fields carry the package version, the process pid, and the store's state directory, and that a request with a non-loopback Host header is refused like every other request
- [x] 1.2 Add a probe module that asks `127.0.0.1:<port>/api/health` with a two-second timeout and classifies the address as `none` (connection refused), `sidescreen` (with version, pid, stateDir), `foreign` (answers but not as sidescreen), or `unanswering` (accepts but does not answer in time); verify with unit tests against a real server from the test helpers, a plain `node:http` server answering other JSON, a port nothing listens on, and a server that accepts the socket and never responds

## 2. The foreground command follows the new rules

- [x] 2.1 On `EADDRINUSE`, make `serve` run the probe and report: a sidescreen server on the same state directory as "already running at <url>" plus the project address, exit 0, honouring `--open`; a sidescreen server on another state directory naming both directories, exit 1; a foreign or unanswering occupant as "in use by something other than sidescreen" with the `--port` suggestion, exit 1; verify by rewriting the existing "second serve on a port already in use" CLI test for exit 0 and the new wording, and adding tests for the foreign and the mismatched-store cases
- [x] 2.2 Add the version note shared by every report of a running server: when the probe's version differs from the package version, append one sentence naming both and saying `sidescreen stop` then `sidescreen start` switches to the installed version; verify with a CLI test against a fake health server that answers as sidescreen with another version and the test's own state directory, asserting the note appears and the exit code stays 0

## 3. The detached start

- [x] 3.1 Add `sidescreen start [--port <n>] [--open]`: refuse port 0 with an explanation; probe and report as in 2.1 when the address is taken; otherwise truncate `<state dir>/server.log`, spawn `process.execPath <bin> serve --port <n>` with `detached: true`, stdin ignored, stdout and stderr on the log file, the current environment and directory, `windowsHide: true`, and `unref()`; then poll the probe every 100 ms for up to five seconds while listening for the child's exit, printing the server and project addresses and exiting 0 on health, re-probing once if the child exits and treating a sidescreen answer as success, otherwise naming the log file with its last lines and exiting 1; verify with CLI tests that `start` on a free fixed port exits 0, prints both addresses, and the address answers health after the command has exited; that port 0 exits non-zero with the explanation; and that a corrupt `store.json` makes the child fail before it binds (`serve` reads the store once and creates the state directory before listening, so a failure cannot race a health answer; a state directory that is a regular file cannot serve as the injection, because `start` itself needs that directory for the log) and `start` name the log file with the reason and exit non-zero
- [x] 3.2 Add the free-port and cleanup helpers to the tests: a helper that listens on port 0, reads the port, and closes, retrying once when a later report says the port is foreign; and a `t.after` that runs `stop --port <n>` and then kills the health-reported pid if the address still answers; verify by running the 3.1 tests twice in a row with no server left listening afterwards
- [x] 3.3 Test survival: run `start` through `sh -c` spawned with `detached: true`, wait for the shell to exit, send SIGTERM to the shell's process group (ignoring ESRCH), and assert the probe still answers as sidescreen with the same pid; verify that the test fails when `detached: true` is removed from the spawn in `start`
- [x] 3.4 Test the race: run two `start` commands concurrently for one free port and assert both exit 0, both print the same address, exactly one pid answers health, and no other `serve` child remains; verify that the test passes repeatedly
- [x] 3.5 Make `--open` work in the found case: compute the project address from the command's own directory and open it whether the server was started or found; verify with a CLI test that puts a directory holding an executable script named after the platform's opener (`open` on macOS, `xdg-open` on Linux) first on PATH, runs `start --open` from a second temporary project directory while a server is already running, and asserts the script was called with that directory's project address

## 4. Stop and status

- [x] 4.1 Add `sidescreen stop [--port <n>]`: probe; on a sidescreen answer send SIGTERM to its pid, treating ESRCH as already stopped, then poll every 100 ms up to five seconds until the connection is refused, exit 0; on `none` print that nothing was running, exit 0; on a foreign or unanswering occupant leave it alone, say so, exit 1; after the bounded wait with the address still answering, print the pid and exit 1 without SIGKILL; verify with CLI tests for a running server (address free afterwards), nothing running, a foreign server that still answers afterwards, and a fake sidescreen-shaped server that ignores SIGTERM, asserting the pid in the message and that it still answers after `stop` returned
- [x] 4.2 Add `sidescreen status [--port <n>]`: one line naming the address, version, and pid with the version note from 2.2 when it applies, exit 0; or one line saying no server is running at the address, exit 1; a foreign occupant is reported as such, exit 1; verify with CLI tests for each of the three cases
- [x] 4.3 Refuse `stop` and `status` for a sidescreen server reading another state directory the same way `start` does, naming both directories and exiting 1, so a command never acts on a server whose store it does not share; verify with a CLI test that starts a server under one `SIDESCREEN_STATE_DIR` and runs `stop` under another, asserting the server still answers

## 5. Skill, help, and documentation

- [x] 5.1 Rewrite the "open the review surface" row of `skills/sidescreen/SKILL.md` to `sidescreen start --open`, say it returns at once and prints the address, add the sentence that a report of a server already running is success and the command is not to be run again in the session, and add a row for `sidescreen stop`; then run `sidescreen init` in this repository so `.claude/skills/sidescreen/SKILL.md` matches; verify with a test that reads the shipped skill and asserts it names `sidescreen start --open`, `sidescreen stop`, and the already-running rule, and that the installed copy is byte-identical to the shipped one
- [x] 5.2 Update the usage text with `start`, `stop`, and `status` and their flags, label `serve` as the foreground command and `start` as the background one in their one-line descriptions, and mention the log file under the environment or a notes section; verify with a CLI test that `--help` lists the three commands and the word "foreground" on the `serve` line
- [x] 5.3 Update the README: quick start becomes install, `sidescreen init`, `sidescreen start --open` with no "keep that terminal open"; the commands table gains `start`, `stop`, and `status`; the busy-port troubleshooting entry says to run `sidescreen status` and explains the foreign-occupant and other-store messages; add where the log file lives, that the server keeps the environment it was started with, to rerun `sidescreen init` in each project after upgrading for the new skill text, and to run `sidescreen stop` before uninstalling; verify by reading the result against every requirement in the `server-lifecycle` delta and the modified `package-distribution` scenario

## 6. End to end and checks

- [x] 6.1 Add an end-to-end test through the real CLI with a temporary state directory: `start` on a free port, then `sidescreen ingest` with the Stop hook fixture as a different process, then `GET /api/turns` on the running server lists the turn, then a second `start` reports the server as running with exit 0 and the same pid, then `stop` frees the address; verify that the test passes headless and leaves no process behind
- [x] 6.2 Confirm the behaviour under Claude Code on this machine: from a Claude Code session in a project with the hooks, run `sidescreen start --open` through the Bash tool, end that session, confirm from a terminal that `sidescreen status` still reports the server and that a turn from a second session appears in the browser, then `sidescreen stop`; record the date, the Claude Code version, and the outcome in this task. Partial outcome, 2026-09-17 with Claude Code 2.1.275: `sidescreen start` ran through the Bash tool of a Claude Code session in this repository and returned in under a second (pid 55880, port 7486, default state directory); the shell that ran it exited; a later shell's `sidescreen status` reported the server, whose parent had become launchd (ppid 1) and whose process group was its own. Not yet exercised from inside that session: ending the session, then `sidescreen status` from a terminal, then a turn from a second session appearing in the browser. Completed by hand on 2026-09-17: the session was ended, `status` from a terminal still reported the server, a later turn appeared in the browser, and `stop` freed the port
- [x] 6.3 Run `npm run check` and confirm lint, typecheck, and all tests pass, and run the lifecycle tests five times in a row to check for flakiness. Outcome, 2026-09-17: check passed with 262 tests, 259 passing and 3 skipped (the opt-in real-CLI tests); the 24 lifecycle and end-to-end lifecycle tests passed in five consecutive runs with no stray server left behind
